import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.davidslatinek.ax87',
    appName: 'ax87',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
